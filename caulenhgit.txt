các câu lệnh console:
cd ../
cd ten-thu-muc/
ls

Các câu lệnh git
git init
git add <tenfile/thumuc>
git add -A
git commit -m "Noi dung ghi chu"
git log
git log --oneline

git checkout -b ten-nhanh-moi
git checkout ten-nhanh
git merge ten-nhanh-can-merge

git checkout master
git merge lao-hac
git merge huong-dan-git